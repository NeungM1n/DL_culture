# 🏛️ CultureFinder (문화재 찾기)

인공지능(AI)을 활용하여 우리 문화재를 쉽고 빠르게 찾고, 상세한 설명을 제공하는 웹 서비스입니다.
사용자가 직접 문화재 사진을 찍거나 업로드하면, 딥러닝 모델이 이를 분석하여 어떤 문화재인지 알려줍니다.

## ✨ 주요 기능 (Features)

*   **📸 실시간 카메라 촬영**: 웹캠이나 모바일 카메라를 통해 바로 문화재를 촬영하고 검색할 수 있습니다.
*   **📂 이미지 업로드**: 소장하고 있는 문화재 사진을 업로드하여 분석할 수 있습니다.
*   **🧠 AI 이미지 분석**: PyTorch 기반의 **ResNet18** 딥러닝 모델이 이미지를 정밀하게 분석합니다.
*   **📖 상세 정보 제공**: 식별된 문화재에 대한 풍부한 설명과 관련 정보를 제공합니다.
*   **🎨 모던한 UI**: Glassmorphism 디자인을 적용하여 깔끔하고 세련된 사용자 경험을 제공합니다.

## 🛠️ 기술 스택 (Tech Stack)

이 프로젝트는 최신 웹 기술과 딥러닝 기술을 융합하여 제작되었습니다.

### Frontend
*   **React**: 사용자 인터페이스(UI) 구축을 위한 JavaScript 라이브러리
*   **Vite**: 빠르고 가벼운 프론트엔드 빌드 도구
*   **Vanilla CSS**: Glassmorphism 디자인 시스템 직접 구현 (외부 라이브러리 의존도 최소화)
*   **HTML5 Camera API**: `navigator.mediaDevices`를 활용한 실시간 카메라 연동

### Backend & AI
*   **Python 3.10+**: 백엔드 및 AI 로직 구현 언어
*   **FastAPI**: 고성능 비동기 Python 웹 프레임워크 (REST API 서버)
*   **PyTorch**: 딥러닝 모델 설계 및 학습 프레임워크
*   **Torchvision**: 이미지 전처리 및 ResNet18 모델 활용
*   **Pillow (PIL)**: 이미지 파일 처리 라이브러리
*   **BeautifulSoup4**: 학습 데이터 수집을 위한 웹 크롤링 도구

## 🚀 시작하기 (Getting Started)

### 1. 환경 설정 (Prerequisites)
*   Node.js (프론트엔드 실행용)
*   Python (백엔드 및 AI 학습용)

### 2. 데이터셋 준비 (Data Acquisition)
AI를 학습시키기 위해서는 문화재 사진 데이터가 필요합니다. 제공된 도구를 사용하면 쉽게 모을 수 있습니다.

1.  `backend` 폴더로 이동합니다.
2.  이미지 다운로드 도구를 실행합니다.
    ```bash
    python download_images.py
    ```
3.  원하는 문화재 이름(예: `gyeongbokgung`)과 장수(예: `20`)를 입력하면 `dataset` 폴더에 자동으로 저장됩니다.
    *   *팁: 비교 학습을 위해 최소 2개 이상의 문화재(클래스)를 다운로드하세요.*

### 3. AI 모델 학습 (Model Training)
데이터가 준비되었다면, 나만의 AI 모델을 학습시킵니다.

```bash
# backend 폴더에서 실행
python train.py
```
*   학습이 완료되면 `culture_model.pth` (모델 파일)와 `class_names.txt` (정답지)가 생성됩니다.

### 4. 서버 실행 (Run Application)

두 개의 터미널을 열어서 각각 실행해야 합니다.

**Terminal 1: Backend (서버)**
```bash
cd backend
uvicorn main:app --reload
```
*   서버가 `http://localhost:8000`에서 실행됩니다.

**Terminal 2: Frontend (화면)**
```bash
# 프로젝트 루트 폴더에서 실행
npm run dev
```
*   브라우저에서 `http://localhost:5173`으로 접속하여 서비스를 이용하세요!

## 📂 프로젝트 구조 (Project Structure)

```
CultureFinder/
├── backend/               # Python 백엔드
│   ├── dataset/           # 학습용 이미지 데이터 저장소
│   ├── main.py            # FastAPI 서버 메인 파일 (API 엔드포인트)
│   ├── model.py           # PyTorch 딥러닝 모델 정의 (ResNet18)
│   ├── train.py           # 모델 학습 스크립트
│   ├── check_dataset.py   # 데이터셋 점검 도구
│   ├── download_images.py # 이미지 자동 다운로드 도구 (크롤러)
│   └── descriptions.json  # 문화재 설명 데이터베이스
├── src/                   # React 프론트엔드
│   ├── components/        # UI 컴포넌트 (LandingPage 등)
│   ├── services/          # API 통신 로직 (aiService.js)
│   ├── index.css          # 전체 스타일 (Glassmorphism)
│   └── App.jsx            # 메인 앱 컴포넌트
└── README.md              # 프로젝트 설명서
```

## 📝 라이선스
이 프로젝트는 학습 및 포트폴리오 목적으로 제작되었습니다.
