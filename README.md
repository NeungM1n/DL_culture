# 🏛️ CultureFinder (문화재 찾기) - AI 기반 문화재 식별 서비스

**CultureFinder**는 딥러닝(Deep Learning)과 생성형 AI(LLM)를 결합하여, 사용자가 촬영한 문화재 사진을 분석하고 상세한 설명을 제공하는 웹 애플리케이션입니다.

단순한 이미지 분류를 넘어, **나만의 데이터셋 구축부터 원격 GPU 서버 학습까지** 경험할 수 있는 올인원 프로젝트입니다.

---

## ✨ 주요 기능 (Key Features)

1.  **📸 실시간 촬영 및 분석**
    *   웹 브라우저에서 카메라를 구동하여 즉시 분석하거나, 고화질 사진을 업로드할 수 있습니다.
2.  **🧠 고성능 AI 모델 (ResNet18 + Custom Training)**
    *   직접 수집하고 증강(Augmentation)한 데이터로 학습된 모델이 문화재를 식별합니다.
3.  **🥈 대안 제시 (Top 3 Alternatives)**
    *   AI가 가장 확신하는 정답뿐만 아니라, 2, 3, 4순위 후보도 함께 보여주어 정확도를 보완합니다.
4.  **💬 AI 도슨트 (LLM Chat)**
    *   **Google Gemini 2.0 Flash** 모델이 탑재되어, 식별된 문화재에 대해 궁금한 점을 자유롭게 물어볼 수 있습니다.
5.  **🎨 Glassmorphism UI**
    *   현대적이고 세련된 글래스모피즘 디자인을 적용하여 몰입감 있는 사용자 경험을 제공합니다.

---

## 🛠️ 기술 스택 (Tech Stack)

*   **Frontend**: React, Vite, Vanilla CSS (Glassmorphism)
*   **Backend**: Python, FastAPI, Uvicorn
*   **AI/ML**: PyTorch (ResNet18), Google Gemini API (LLM)
*   **Tools**: icrawler (Image Crawling), Torchvision (Augmentation)

---

## 📂 프로젝트 파일 구조 (File Structure)

### 1. Backend (`/backend`)
*   **`main.py`**: FastAPI 서버. 이미지 분석(Top 4 예측) 및 LLM 채팅 기능을 제공합니다.
*   **`train.py`**: AI 학습 스크립트. 데이터 증강, 클래스 가중치 적용, 정확도 계산 기능이 포함되어 있습니다.
*   **`download_images.py`**: **[New]** `icrawler`를 사용하여 고품질 이미지를 대량으로 자동 수집합니다.
*   **`augment_dataset.py`**: **[New]** 부족한 데이터를 회전/반전/색변환하여 목표 수량(예: 200장)까지 자동으로 채워줍니다.
*   **`descriptions.json`**: 문화재별 상세 설명 데이터베이스입니다.

### 2. Frontend (`/src`)
*   **`LandingPage.jsx`**: 카메라 촬영 및 파일 업로드 UI.
*   **`ResultPage.jsx`**: 분석 결과 및 대안(Top 3) 표시 UI.
*   **`ChatInterface.jsx`**: AI와의 실시간 채팅 UI.

---

## 🚀 데이터셋 구축 가이드 (Dataset Guide)

나만의 AI를 만들기 위해 데이터를 수집하고 가공하는 방법입니다.

### 1. 이미지 수집 (Crawling)
```bash
cd backend
python download_images.py
# 검색어(예: 숭례문)와 수량(예: 50)을 입력하면 자동으로 다운로드됩니다.
```

### 2. 데이터 증강 (Augmentation)
데이터가 부족해도 걱정 없습니다. 이 도구가 목표 수량까지 이미지를 뻥튀기해줍니다.
```bash
python augment_dataset.py
# 목표 수량(예: 200)을 입력하면 모든 클래스를 200장으로 맞춰줍니다.
```

---

## ☁️ 원격 GPU 서버 학습 가이드 (Remote Training)

로컬 컴퓨터보다 훨씬 빠른 GPU 서버에서 학습하는 방법입니다.
*(보안을 위해 구체적인 IP와 포트는 제외했습니다. 본인의 서버 정보를 사용하세요.)*

### 1. 🔌 서버 접속 (SSH)
```bash
ssh -p [PORT] [USERNAME]@[SERVER_IP]
# 예: ssh -p 22 student@123.456.78.90
```

### 2. 📂 파일 전송 (내 컴퓨터 -> 서버)
프로젝트 파일(`backend` 폴더)을 서버로 보냅니다. **(새 터미널에서 실행)**
```bash
scp -P [PORT] -r backend [USERNAME]@[SERVER_IP]:~/culture_project
```

### 3. 🔥 학습 실행 (서버)
서버에 접속한 상태에서 실행합니다.
```bash
# 폴더 이동
cd ~/culture_project

# 필수 라이브러리 설치 (최초 1회)
pip3 install -r requirements.txt

# 학습 시작 (30 Epoch)
python3 train.py
```

### 4. 💾 모델 다운로드 (서버 -> 내 컴퓨터)
학습된 모델(`culture_model.pth`)과 이름표(`class_names.txt`)를 가져옵니다. **(내 컴퓨터 터미널에서 실행)**
```bash
# 모델 파일 가져오기
scp -P [PORT] [USERNAME]@[SERVER_IP]:~/culture_project/culture_model.pth ./backend/

# 클래스 이름 파일 가져오기
scp -P [PORT] [USERNAME]@[SERVER_IP]:~/culture_project/class_names.txt ./backend/
```

---

## 🏃‍♂️ 실행 방법 (How to Run)

### 1. Backend 실행
```bash
cd backend
uvicorn main:app --reload
```

### 2. Frontend 실행
```bash
npm run dev
```

이제 브라우저에서 `http://localhost:5173`으로 접속하여 서비스를 이용해보세요!
